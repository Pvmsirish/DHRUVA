"""Day 4 — Fleet (stub; day 5 completes it).

Concept: a fleet runs several harnesses in parallel — one per task, each
in its own directory — and gathers their reports.  The module exists today
so the package layout is final; day 5 fills in the implementation.
"""

from __future__ import annotations


def run_fleet(tasks: list[str], workdir: str = ".") -> list[str]:
    """Run one harness per task and return their reports (day 5)."""
    raise NotImplementedError("fleet arrives on day 5")
